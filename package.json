{
  "name": "serverless-node-api",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "offline": "npx serverless offline --stage dev",
    "install:plugin": "npx serverless plugin install --name"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.5",
    "drizzle-orm": "^0.33.0",
    "express": "^4.19.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.24.2",
    "serverless-offline": "^14.3.2"
  }
}
